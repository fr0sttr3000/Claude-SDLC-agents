---
description: Подготовить append-only supersede/tombstone Proposal v1 для stale записей
---

Для Project `$ARGUMENTS` сравни exact approved memory snapshot с current defect artifacts и
создай только Proposal v1 TSV в явно заданный output path. Используй `supersede|tombstone`
только при точном target record id и подтверждающем Project source digest. Не изменяй provider,
Project defect artifacts, approvals или gates.
