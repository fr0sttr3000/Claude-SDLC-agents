---
date: 2026-06-01
tags: [release, v2.000.001, patch]
version: 2.000.001
---

# Release Notes — v2.000.001

**Тип:** Patch (один большой багфикс — переносимость, доведение лаунчера, прод-правила)
**Дата:** 2026-06-01
**Ветка:** feat/v2.000.000
**Базируется на:** v2.000.000

---

## Кратко

Багфикс-релиз поверх 2.000.000. Новой функциональности для пользователя нет — это исправления, которые делают систему переносимой между окружениями, доводят лаунчер до объявленной в 2.000.000 модели 3 циклов и переносят правила из прод-инцидентов FamilyPlannerBot в агентов.

---

## Что изменилось

### Исправлено

#### Переносимость — убраны захардкоженные абсолютные пути (BLOCKER)

**Проблема:** пути `/home/host-gui-car/...` были зашиты в лаунчеры, во все `CLAUDE.md` агентов и в документацию. Vault не работал на чужой машине без массового переписывания путей.

**Исправление:**
- `sdlc.sh` и `localrun.sh` вычисляют пути от расположения скрипта:
  ```bash
  AGENTS="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  VAULT="$(dirname "$AGENTS")"
  ```
- `PATH` использует `$HOME/.local/bin` вместо `/home/host-gui-car/.local/bin`
- Лаунчеры экспортируют `SDLC_VAULT` и `LOCALRUN_PROJECTS` в окружение агентов
- Во всех `CLAUDE.md` и slash-командах агентов абсолютные пути заменены на `$SDLC_VAULT` / `$AGENT_DIR`
- В документации пути заменены на env-переменные или `<vault-root>`
- В `CLAUDE.md` добавлен раздел «Пути проекта (env-переменные)» с таблицей переменных и фолбэком: если переменная пуста — спросить путь у пользователя, не угадывать

#### `localrun.sh` — настраиваемый путь к локальным проектам

**Проблема:** каталог локальных проектов был зашит как `/home/host-gui-car/Projects/claude`.

**Исправление:** путь определяется по приоритету `env LOCALRUN_PROJECTS` → config-файл `~/.config/sdlc-agents/config` → first-run wizard. При первом запуске мастер спрашивает каталог и сохраняет его в config.

#### `sdlc.sh` — лаунчер приведён к модели 3 циклов

В 2.000.000 была объявлена архитектура из 3 циклов, но `sdlc.sh` всё ещё гонял монолитный цикл со смешанными агентами Циклов 1/2/3.

- `CYCLE_AGENTS` разделён на `CYCLE1_AGENTS` (22 шага), `CYCLE2_AGENTS`, `CYCLE3_AGENTS`
- Из Цикла 1 убраны шаги Циклов 2/3 (`s4-devops`, `s6-release`, `s6-sre`); Цикл 1 завершается `s0-tracker:/report` + `s0-github:/push`
- `gate7` убран из необязательных шагов (его место — Цикл 3)
- Циклы 2/3 — заглушки «⏳ в разработке» с перечнем запланированных агентов

#### Правила из прод-инцидентов FamilyPlannerBot

- `CLAUDE.md` — раздел «Поведенческие правила агентов»: git не для отката (INC-02), запись файлов самостоятельно без делегирования сабагентам (INC-03), «все» = полный вывод (INC-05), учёт deployment constraint (INC-07), верификация директории перед записью (INC-01)
- `s4-dev/CLAUDE.md` — `assert` запрещён в production-коде (INC-08), удаление неиспользуемых импортов, `server_default` только строковый литерал (CR-01), функциональные индексы только на IMMUTABLE-выражениях; добавлены пункты DoD-чеклиста
- `s4-techlead/CLAUDE.md` — `[BLOCKER]` на `assert` в проде, `[MINOR]` на неиспользуемые импорты, правило выпуска процессных артефактов `PROC-*` в фазе разработки (INC-06)

### Изменено

#### Навигация и UX лаунчеров

- В `sdlc.sh` и `localrun.sh` во все меню добавлен пункт `b) Назад`
- Главное меню `sdlc.sh`: пункт 1 — «Запустить цикл» (подменю Разработка / Деплой / Эксплуатация / Всё сразу), пункт 2 — «Запустить один агент»
- Меню одиночного запуска сгруппировано по циклам + Tools + Local Run
- Пустой ввод имени проекта трактуется как отмена, а не ошибка

---

## Затронутые файлы

| Файл | Тип изменения |
|------|--------------|
| `sdlc.sh` | Fixed: пути от BASH_SOURCE; разделение на CYCLE1/2/3_AGENTS; подменю циклов; навигация «Назад» |
| `localrun.sh` | Fixed: пути от BASH_SOURCE; настраиваемый LOCALRUN_PROJECTS + first-run wizard; навигация «Назад» |
| `CLAUDE.md` | Updated: env-пути; раздел «Пути проекта»; «Поведенческие правила агентов» (INC) |
| `cycle1-dev/s4-dev/CLAUDE.md` | Added: правила assert/imports/server_default/IMMUTABLE + DoD |
| `cycle1-dev/s4-techlead/CLAUDE.md` | Added: BLOCKER на assert, PROC-* артефакты (INC-06) |
| Все остальные `CLAUDE.md` / команды агентов | Fixed: абсолютные пути → `$SDLC_VAULT` / `$AGENT_DIR` |
| `OVERVIEW.md`, `README.md`, `GETTING_STARTED.md`, `plans/roadmap.md` | Updated: меню, 22 шага Цикла 1, нумерация пунктов, `CYCLE_AGENTS`→`CYCLE1_AGENTS` |
| `CHANGELOG.md` | Added: секция 2.000.001 |

---

## Обновление

```bash
git pull origin feat/v2.000.000
# или
bash sdlc.sh   # уже включает все исправления
```

---

## Совместимость

Полная обратная совместимость. Новых зависимостей нет. Существующие проекты не затронуты. При первом запуске `localrun.sh` спросит путь к каталогу локальных проектов (или возьмёт его из `LOCALRUN_PROJECTS`).
